#!/bin/sh

# Sets and samples the release environment.
# This is evaluated as a script by the `bin/RELEASE_NAME` entrypoint.

# Set the release to work across nodes.
# RELEASE_DISTRIBUTION must be "sname" (short), "name" (long), or "none".
export RELEASE_DISTRIBUTION=name
export RELEASE_NODE=lattice@127.0.0.1
